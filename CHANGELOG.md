## 0.2.0

- 将项目重新定位为特殊文件预览库，并完成品牌、示例与资源脱敏。
- 使用仓库脚本生成的中性图集和提示音替换原有素材。
- 保留独立实例 C ABI v1、yyjson 解析器和动态零拷贝帧缓冲区。
- 新增 `PluvioraSource`、`PluvioraPlayer`、`PluvioraController` 与高级 `PluvioraEngine` API。
- 支持 flutter_soloud 音频主时钟、AVIF 背景图、多层覆盖素材、移动图集和命中 Fragment Shader。
- 包含原生 Sanitizer、Flutter Golden 与移动端构建 CI。
