name: pluviora_example
description: Pluviora special-file preview example.
publish_to: none
version: 0.1.0+1

environment:
  sdk: '>=3.10.8 <4.0.0'

dependencies:
  file_picker: ^11.0.3
  flutter:
    sdk: flutter
  pluviora:
    path: ../

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
